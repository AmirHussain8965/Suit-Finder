## Packages
react-leaflet | Map components
leaflet | Core map library
clsx | Class name utility (if not already present)
tailwind-merge | Tailwind class merging (if not already present)
framer-motion | Smooth animations for dialogs and transitions

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'DM Sans'", "sans-serif"],
  serif: ["'Playfair Display'", "serif"],
  display: ["'Playfair Display'", "serif"],
}
Colors:
primary: Navy/Deep Blue
accent: Gold/Brass
background: Charcoal/Dark Gray
